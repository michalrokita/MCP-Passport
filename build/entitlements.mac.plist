<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the signed/notarized macOS build.
  Applied only in CI when CSC_LINK is set — local dev builds skip this.

  Why each one:
  - allow-jit + allow-unsigned-executable-memory + allow-dyld-environment-variables:
    Electron's V8 needs JIT; without these the app crashes immediately under
    hardened runtime.
  - cs.disable-library-validation:
    Lets Electron load the bundled Frameworks/Electron Helper binaries that we
    didn't sign with the same Team ID as the main app (we did, but Electron's
    helpers historically need this in case future updates change ABI).
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
